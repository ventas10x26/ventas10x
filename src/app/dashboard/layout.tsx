export const metadata = {
  title: 'Ventas10x — Dashboard',
  description: 'Panel de control Ventas10x',
}

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
